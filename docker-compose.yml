version: '3.8'

services:
  access-control-api:
    build: .
    container_name: access-control-api
    ports:
      - "8082:8082"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    restart: always
